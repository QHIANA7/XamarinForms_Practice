<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DPTIS_XamarinF_PR2.Views.SearchBarPR_Page"
             Title="SearchBar"
             Padding="10,0">
    <StackLayout>
        <Label Text="SearchBar"
               FontSize="50"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <Label Text="SearchBar는 문자열을 입력할 수 있는 공간을 제공하며 응용 프로그램에 검색 수행을 알리는 버튼 또는 키보드 키를 표시합니다. Text 속성은 입력한 문자열에 대한 접근을 제공하며 SearchButtonPressed 이벤트는 검색 단추를 눌렀을 때 발생합니다."/>
        <Label Text="SearchBar 컨트롤은 Placeholder 텍스트, 쿼리 입력, 검색 실행 및 취소를 지원합니다. 다음 스크린 샷은 ListView에 결과가 표시된 SearchBar 쿼리를 보여줍니다."/>

        <SearchBar Placeholder="Xamarin.Forms Property"
                   SearchButtonPressed="OnSearchBarButtonPressed" />

        <ScrollView VerticalOptions="FillAndExpand">
            <Label x:Name="resultsLabel" />
        </ScrollView>
    </StackLayout>
</ContentPage>